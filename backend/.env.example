# ============================================================================
# Backend 环境变量配置模板
#
# 用法：复制为 .env 后填入真实值（.env 已被 .gitignore 忽略）。
#       本模板只放占位符，切勿写入真实密钥。
# ============================================================================

# ---------------------------------------------------------------------------
# 服务器
# ---------------------------------------------------------------------------
SERVER_HOST=0.0.0.0
SERVER_PORT=8080
SERVER_MODE=debug              # debug | release

# ---------------------------------------------------------------------------
# 数据库（PostgreSQL）
# ---------------------------------------------------------------------------
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=123456
DB_NAME=effchat
DB_SSLMODE=disable             # disable | require | verify-full

# ---------------------------------------------------------------------------
# JWT
# ---------------------------------------------------------------------------
# 必须改为强随机值：SERVER_MODE=release 时若仍为默认值，服务将拒绝启动。
JWT_SECRET=your-secret-key-change-this-in-production

# ---------------------------------------------------------------------------
# 业务运行配置
#
# 模型渠道、API key、搜索 provider 和网页提取服务统一在管理员网页配置，
# 保存到 PostgreSQL 后对新请求实时生效；本文件不再提供这些业务配置。
# ---------------------------------------------------------------------------

# ---------------------------------------------------------------------------
# 反向代理 / CORS / 安全
# ---------------------------------------------------------------------------
# 只在可信反向代理会清洗 X-Forwarded-* 请求头时开启。
TRUST_PROXY_HEADERS=false

# 前后端不同源部署时填精确来源，多个用英文逗号分隔。
# 例如：https://chat.example.com,http://192.168.1.10:8088
CORS_EXTRA_ORIGINS=

# ---------------------------------------------------------------------------
# SSE / Agent run 时间控制
#
# 时长支持 Go duration（90s、25m）或纯秒数。设为 0 时保持聊天 15 分钟、压缩 5 分钟
# 的默认总时长；不会无限等待上游模型。
# ---------------------------------------------------------------------------
RUN_MAX_TOTAL_DURATION=0
SSE_HEARTBEAT_INTERVAL=12s

# ---------------------------------------------------------------------------
# 登录限流（进程内，适合小团队自托管防暴力尝试）
# ---------------------------------------------------------------------------
AUTH_RATE_LIMIT_MAX_ATTEMPTS=10
AUTH_RATE_LIMIT_WINDOW_SECONDS=600
AUTH_RATE_LIMIT_BLOCK_SECONDS=900

# ---------------------------------------------------------------------------
# Python 文档解析服务（本地直跑 backend 时通常用 localhost）
# Docker 部署请使用根目录 .env.docker，里面的地址是 http://py-extractor:8090。
# ---------------------------------------------------------------------------
PY_EXTRACTOR_ENABLED=true
PY_EXTRACTOR_URL=http://localhost:8090
PY_EXTRACTOR_TIMEOUT_SECONDS=60

# ---------------------------------------------------------------------------
# 对话压缩（eino summarization 中间件，运行时生效）
# 历史超阈值时，把更早的消息摘要为一条上下文再喂给模型——只影响"本次请求发给
# 模型的上下文"，数据库原始消息全量保留、可回溯。摘要复用当前对话模型。
# 设为 0 则关闭自动压缩。
# ---------------------------------------------------------------------------
COMPRESSION_MAX_TOKENS=32000
